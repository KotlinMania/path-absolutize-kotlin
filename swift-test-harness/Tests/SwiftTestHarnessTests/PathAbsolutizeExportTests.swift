import Testing
import PathAbsolutize

@Suite("PathAbsolutize Export Smoke Tests")
struct PathAbsolutizeExportTests {
    @Test("Swift module loads cleanly")
    func testSwiftModuleLoads() throws {
        #expect(true)
    }
}
